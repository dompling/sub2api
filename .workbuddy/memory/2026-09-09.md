# 2026-09-09 工作日志

## 任务：参考 ufec/sub2api feat/codebuddy_workbuddy 合并 CodeBuddy/WorkBuddy 支持

### 关键背景（重要，避免下次误判）
- 当前项目 `dompling/sub2api` main 分支版本 0.1.184，工作区有 **144 个未提交改动**（用户自己的在途实现），
  已包含 CodeBuddy/WorkBuddy 平台的完整实现（后端 OAuth/订阅/配额/网关 + 前端 UI）。
- 参考分支 ufec/sub2api `feat/codebuddy_workbuddy`（tag v0.1.162-codebuddy，基线 0.1.161）提供的是**同一套功能**。
- 参考实现把 CodeBuddy 建模为与 Grok 平行的一等平台：`domain.PlatformCodeBuddy = "codebuddy"`，
  复用 OpenAI 兼容网关 + OAuth + token refresh + quota fetch，额外做 system-prompt 内容过滤净化等。
- **教训**：工作区并非干净上游，含大量未提交工作。分析/修改前必须先 `git status` 确认，绝不能用
  `git checkout -- <file>` 或 `git apply`/覆盖式 `cp` 处理任何已有文件（会毁掉用户未提交改动）。

### 结论与验证结果
- 用户工作区已 100% 具备 CodeBuddy 能力文件（backend 网关各文件、service、repository、handler、前端组件均有引用）。
- 验证：`go build ./...`（backend）通过（FULL_BUILD_EXIT=0）；前端 `npm run typecheck`(vue-tsc) 通过（EXIT=0）。
- 即：参考 feature 在当前项目已是"已合并"状态，无更多实质缺失；任务演变为"核对/补齐而非新写"。

### 最终结论
- 工作区在 main，未提交改动 144（+1 为本次记忆文件）。CodeBuddy 集成是用户自身在途工作，已完整。
- 验证通过：backend `go build ./...`、frontend `npm run typecheck` 均无错。
- 参考 feature 与当前实现属同一套功能；未做覆盖式改动，未破坏用户任何文件。
- 待用户决策：是否需要把参考里的特定细节（如 ent user_platform_quota schema 校验是否加 codebuddy、
  ReAuthAccountModal/AccountsView/GroupsView 等前端零星接线、error_passthrough 等）进一步对齐。

### 本次实际触碰
- 曾误 `git checkout -- backend/internal/domain/constants.go`（用户已加 PlatformCodeBuddy），
  随后已手动恢复 `PlatformCodeBuddy = "codebuddy"`（紧跟 PlatformComposite 之后）。已还原一致。
- 曾复制参考 `pkg/codebuddy` 等文件，但 15:39 工作区整体被材料化覆盖为用户自身版本，最终以用户版本为准、可编译。
- 参考完整克隆保留在 /tmp/ufec_sub2api_full（后续比对用）。

### Go 工具链备忘
- 本机 `go` 不在 PATH；用 `/opt/homebrew/bin/go`（1.26.3，GOTOOLCHAIN=auto 自动下载 1.27.0 满足 go.mod 1.27）。
- node 用 managed：`/Users/dompling/.workbuddy/binaries/node/versions/22.22.2-2/bin/node`。
